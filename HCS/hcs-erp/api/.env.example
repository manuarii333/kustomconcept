# HCS ERP — api/.env (template)
# Copier ce fichier en api/.env sur le serveur (jamais commité — voir .gitignore)
# Le mot de passe MySQL doit être renseigné en clair ici, lu uniquement par PHP côté serveur.

DB_HOST=localhost
DB_PORT=3306
DB_NAME=highftqb_HCS_ERP
DB_USER=highftqb_ERP
DB_PASS=REMPLACER_PAR_LE_NOUVEAU_MDP_MYSQL
API_KEY=hcs-erp-2026
